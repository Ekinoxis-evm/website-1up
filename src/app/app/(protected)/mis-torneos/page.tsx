import { MisTorneosTab } from "@/components/perfil/MisTorneosTab";

export const metadata = { title: "Mis Torneos — 1UP App" };

export default function AppMisTorneosPage() {
  return <MisTorneosTab />;
}
