import { ProfilePage } from "@/components/perfil/ProfilePage";

export const metadata = { title: "Mi Perfil — 1UP Gaming Tower" };

export default function PerfilRoute() {
  return <ProfilePage />;
}
